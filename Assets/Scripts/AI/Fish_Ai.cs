using Unity.VisualScripting;
using UnityEngine;

public class Fish_Ai : MonoBehaviour
{
    /*Probably they'll want to mostly avoid pollution, and if they are covered in grime, to act different than when clean.
      If we had coral set up already, maybe have fish drawn to various types of coral, possibly based on the type of fish.*/

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
